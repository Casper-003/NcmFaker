<Page
    x:Class="NcmFaker.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NcmFaker.Views">

    <Grid Padding="30">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="30" MaxWidth="800" HorizontalAlignment="Left">

                <TextBlock Text="设置" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Spacing="10">
                    <TextBlock Text="外观" FontWeight="SemiBold" FontSize="16"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel Spacing="15">
                            <TextBlock Text="选择你偏好的应用主题颜色。" Foreground="Gray" FontSize="12"/>
                            <RadioButtons x:Name="ThemeRadioButtons" SelectionChanged="ThemeRadioButtons_SelectionChanged">
                                <RadioButton Content="跟随系统设置" Tag="Default"/>
                                <RadioButton Content="浅色主题 (Light)" Tag="Light"/>
                                <RadioButton Content="深色主题 (Dark)" Tag="Dark"/>
                            </RadioButtons>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="10">
                    <TextBlock Text="性能" FontWeight="SemiBold" FontSize="16"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel Spacing="15">
                            <TextBlock Text="最大任务并发数" FontWeight="SemiBold"/>
                            <TextBlock Text="调整同时进行解密或歌词匹配的文件数量。适当调低可保证轻薄本运行流畅，性能强劲的设备可调高以获得极限速度。" 
                                       Foreground="Gray" FontSize="12" TextWrapping="Wrap"/>
                            <Slider x:Name="ConcurrencySlider" 
                                    Minimum="1" Maximum="8" Value="2" 
                                    TickFrequency="1" TickPlacement="BottomRight" 
                                    Width="300" HorizontalAlignment="Left"
                                    ValueChanged="ConcurrencySlider_ValueChanged"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="10">
                    <TextBlock Text="输出偏好" FontWeight="SemiBold" FontSize="16"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel Spacing="15">
                            <TextBlock Text="NCM 文件转换后的输出目录行为。" Foreground="Gray" FontSize="12"/>
                            <RadioButtons x:Name="OutputRadioButtons" SelectionChanged="OutputRadioButtons_SelectionChanged">
                                <RadioButton Content="在源目录下新建 transferred 文件夹存放 (推荐)" Tag="Subfolder"/>
                                <RadioButton Content="直接输出到源文件同级目录 (覆盖式混合)" Tag="SameFolder"/>
                            </RadioButtons>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>